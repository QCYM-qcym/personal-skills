# 1.10.0 生命周期行为验证

2026-09-25。两个未参与编写的助手只读加载当前Skill及相关引用，输出真实教学回复；输入为合成场景，不是假装已有用户课程记录。自动检查与行为结果分开。本文件也是后续可重跑的场景集。

## 13项最低验收与输入

| ID | 场景 | 要观察的行为 |
| --- | --- | --- |
| T1 | 旧schema1已有未答题、L2提示、即时独立节点、custom_color未知字段；续学 | 保留原任务/提示/证据/未知字段，不重新开课 |
| T2 | 只有概率论目录，想3小时过全课 | 临时主线、模块、模型/公式含义、依赖、易混与队列；不谎称已读400页 |
| T3 | 总览时Z=g(X,Y)没感觉，用户先继续 | 非阻塞入深挖队列，短解释后继续 |
| T4 | 地图回答“期望决定唯一分布，所以分布在期望后” | 局部地图修复，不标总览检查通过 |
| T5 | 齐次泊松过程等待下次故障判泊松；提示后改指数 | 诊断→最小修复→新复测；订正不算独立 |
| T6 | 简单题对或用户说懂了 | 不升级迁移/稳定掌握，选择最小验证 |
| T7 | 连续有标签题正确 | 中性无标签混练，题目和教师答案分离，记录上下文提示 |
| T8 | 独立与不相关逆向；方差和漏条件 | 条件/反例训练，有限二阶矩；独立不当必要条件 |
| T9 | Teach Back说方差就是平均值 | 检查内部模型，纠正概念边界，不只复述标准答案 |
| T10 | 有限总体不放回选二项 | 定位模型识别/独立条件，而非全章重讲 |
| T11 | 递归学习 | base/recursive/smaller/return、先预测与用户核心实现、边界测试路线 |
| T12 | 明确要3红2白不放回取2全红直接答案 | 给3/10与依据，不强制先答题 |
| T13 | 普通翻译The sample is small. | 直接翻译，不启动学习档案 |

## 执行结果

两个独立助手分别执行T1–T4/T6/T12/T13和T5–T11；13项回复式验证通过。以下保存实际输出的关键片段及判断，而非假定用户已经学会。

| 场景 | 实际回复/观察 | 结果 |
| --- | --- | --- |
| T1 | 缺原题原文时不假装恢复；保留schema1、L2、未知字段和旧能力范围，阶段只能按证据推断 | 只读兼容审查通过，文件验证另列 |
| T2 | “哪些结果会发生→给数值结果分配概率→概括分布→重复试验规律”；给30/50/40/40/20分钟分配、条件概率/分布函数/期望/方差含义、依赖及易混；明说只有目录 | 全景覆盖通过，未读取真实教材 |
| T3 | 用Z=X+Y解释把两个随机数合成一个，指出一般需联合分布；入NEEDS_EXAMPLE队列后继续期望 | 非阻塞继续通过 |
| T4 | X恒为1，Y等概率0/2：期望同为1但分布不同；让用户只修复该边 | 最小地图修复，未标通过 |
| T5 | “这里问下一次故障前等待多久，你所选分布的变量表示什么？”；订正后给速率4每小时等待超过15分钟的新题 | H1修复，后题contextual，未标独立迁移 |
| T6 | “平均通勤时间一样，分布一定一样吗？”；简单题对只记当前应用 | 未升级掌握，通过 |
| T7 | 4个中性题号：不放回抽样、故障过程、X与X²、独立部件失效；不附答案，并先做第1题 | 混练可执行，仍保守记contextual |
| T8 | 对方差等式给X等概率±1且Y=X的反例任务；独立/不相关要求构造逆向反例 | 条件与反例通过，未泄露完整解 |
| T9 | 区分偏差平方平均与原数据均值，用4/5/6与0/5/10让学生重新解释 | 关键模型修复，未标TEACHABLE |
| T10 | “不放回取出目标对象后，第二次概率怎样变化？” | 先检查模型条件，不先报超几何答案 |
| T11 | 非负n求和，n=0停止；先预测n=3调用及返回关系，后续由用户实现并测0/1/4 | 未直接给代码，未伪造运行结果 |
| T12 | 直接给(3/5)(2/4)=3/10与不放回依据 | 直接回答路径通过 |
| T13 | “样本量很小。” | 非学习请求通过 |

执行风险检查：H0仅表示本题没有新提示，不自动意味着cue_level=none；中性题号与答案隐藏不自动证明陌生迁移。旧L提示没有统一新scheme值，保留原标识与原文即可，不能擅写H-v1。新增phase缺证据时明确推断，不由L2推断用户已经犯错。

本版清理了教学适配、项目启动和Obsidian分支的旧默认冲突；两个验证助手均加载了当前主入口及相关扩展。未进行真实400页PDF阅读、真实长期学习、真实音频或新HTML交互测试；场景里的学生话语均为给定测试输入。


## 真实临时文件接续测试

独立助手在系统临时目录构造旧课程夹具：schema1、revision7、project_id=fixture-course、L2、未知custom_color和用户注释、原未答条件概率题、原节点即时独立通过及旧事件。输入“我把条件方向看反了，先继续下一节”为合成测试，不是真实用户学习证据。

实际执行新事件→MISSION待回访队列→CURRENT最后提交→读回，并第二次独立读取。14项断言通过：revision成为8；schema/project_id不变；未知字段和注释保留；旧题全文/ID/L2及跳过原因可恢复；新阶段有实际讲授事件依据；无新题pending_task为空；旧提示注明属于旧题；唯一next_action可执行。NODES及旧事件哈希前后相同，没有伪造独立/迁移/延迟通过。

夹具和临时执行文件留在系统临时目录，不纳入本仓库；本次不是自动化教学引擎测试。并发写入、中断恢复、真实跨设备同步未实测。

## 自动检查

scripts/validate_skills.py通过（4个Skill及本地引用）；unittest发现6项全部通过；skill-creator quick_validate通过；git diff --check无错误。行为场景为agent实际回复和临时文件操作验证，不能将其称为已证明长期教学效果。
